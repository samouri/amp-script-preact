{
  "name": "jake",
  "version": "1.0.0",
  "description": "",
  "main": "parceltest.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build example.js && echo 'var ' | cat - dist/example.js > temp && mv temp dist/example.js",
    "serve": "http-server --cors"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.12.1",
    "parcel": "^2.8.2",
    "preact": "^10.3.3"
  }
}
