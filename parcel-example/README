## To Run

```
npm i
npm run build
npm run serve
```

## Warning

The build step adds in a total hack to get around how parcel doesn't want to be used in a module environment. 
See https://github.com/parcel-bundler/parcel/issues/1401
